# Prompt 000A — VS Code Claude Code Session Start

You are running inside the Claude Code VS Code extension.

First:
1. Read `CLAUDE.md`.
2. Read `README_VSCODE_HE.md`.
3. Read `.claude/settings.json`.
4. Read `docs/03_ROADMAP.md`.
5. Run only:
   `pwsh -NoProfile -File scripts/Invoke-AgencyOS.ps1 doctor`

Do not implement code yet.

Return:
- active milestone;
- repository/toolchain state;
- whether the workspace is ready for M0;
- any settings/configuration warning;
- the exact next prompt file to @-mention.

Do not alter VS Code settings, Claude permission mode, or user-level configuration.
